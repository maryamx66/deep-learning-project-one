# Rümeysa Akı

## Responsible for the evaluation metrics:

- Loaded the optimal model weights (`best_model_zehra.pth`) to perform inference on the test dataset without gradient calculations.

- Calculated the overall model accuracy and generated a comprehensive classification report detailing precision, recall, and F1-scores for each digit.

- Generated and saved a visual confusion matrix heatmap to identify misclassification patterns between specific digits.

- Extracted and plotted a grid of misclassified image samples to visually analyze the model's failure modes.
